# Base URL of apps/server's Hono API. The admissions inquiry form (the
# /admissions page and the navbar "Apply Now" dialog) posts to
# `${NEXT_PUBLIC_API_URL}/admissions` directly from the browser.
NEXT_PUBLIC_API_URL=http://localhost:3000
